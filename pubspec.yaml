name: fokuskripto
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  intl: ^0.19.0
  shared_preferences: ^2.1.0
  crypto: ^3.0.3
  image_picker: ^1.0.7 
  path_provider: ^2.1.3
  geolocator: ^11.0.0 
  geocoding: ^3.0.0  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
